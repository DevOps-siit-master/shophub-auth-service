# Each PR requires review/approval from a code owner (DevOps requirement 5.1).
# Replace the placeholder with real GitHub handles (a teammate is needed so a
# different person than the PR author can approve).
* @adamoviccj
